---
title: break를 썼는데 왜 마지막 문장이 실행될까?
description: 프로그램을 끝내려던 질문에서, 반복문이 끝나는 이유를 구분하는 for–else로 이어진 기록.
date: 2026-09-18
published: 2026-09-22
publish: true
type: journal
tags: [python]
sources:
  - 2026-09-18 개인 Python 학습 대화의 반복문 종료 질문 (공개용 발췌·재구성)
  - https://docs.python.org/3/tutorial/controlflow.html#else-clauses-on-loops
---

숫자 맞히기 문제에서 정답을 맞히면 `break`를 실행했다. 그런데 반복문 아래에 둔 실패 안내도 실행되는 구조였다. 그때는 프로그램 전체를 종료해야 하는지 물었다.

이 글은 9월 18일 학습 대화 중 이 질문을 골라 9월 22일 정리한 기록이다. 그날의 모든 학습 내용을 옮긴 것은 아니다.

## 질문: break 대신 프로그램 전체를 종료해야 할까?

당시 질문의 요지는 “정답을 맞혔으니 실행을 끝내고 싶은데, `break` 대신 무엇을 써야 할까?”였다.

**실제로 필요했던 동작은 실패 안내를 건너뛰는 것**이었다. `break`는 가장 가까운 반복문을 빠져나온다. 반복문 뒤에 있는 문장을 없애거나 건너뛰지는 않는다.

아래는 당시 코드의 제어 흐름을 짧게 재구성한 예시다. 과제 원문을 그대로 옮긴 코드는 아니다.

```python
for guess in [3, 7]:
    if guess == 7:
        print("정답")
        break

print("찾지 못함")
```

`7`에서 `break`를 만나도 마지막 `print`가 반복문 밖에 있으므로 두 문장 모두 출력된다.

## 설명에서 달라진 기준

대화에서는 **“실행을 완전히 끝내기”에서 “반복이 끝난 이유를 구분하기”로 해결 위치를 바꿨다.** 실패 안내를 `for`에 붙은 `else`로 옮기면 된다.

```python
for guess in [3, 7]:
    if guess == 7:
        print("정답")
        break
else:
    print("찾지 못함")
```

`else`는 `if`가 아니라 `for`와 같은 들여쓰기다. `break`로 나가면 건너뛰고, 반복할 항목을 모두 소진하면 실행한다. 자세한 조건은 [[python/loop-exit|반복문 종료와 for–else]]에 모았다.

## 직접 확인할 부분

위 설명이 제공된 사실은 대화에서 확인했다. **수정한 코드를 직접 실행했는지, 혼자 설명할 수 있게 됐는지는 해당 기록에서 확인되지 않았다.** 다음 복습에서는 출력과 그 이유를 먼저 예측한 뒤 비교한다.

- [[python/loop-exit#퀴즈-break-뒤의-코드는-실행될까|break 뒤 코드의 실행 예측]]
- [[python/loop-exit#퀴즈-반복할-항목이-없으면-else는-실행될까|빈 반복문의 else 예측]]

실행을 멈추는 범위와 실행 상태를 지우는 범위도 다르다. [[python/notebook-state|노트북 이름 공간]]에서 셀 실행과 커널 재시작의 차이를 이어 본다.

## 근거

- 개인 학습 대화: 2026-09-18. 질문과 제공된 설명을 확인했으며, 원문 대화·개인 노트북 주소는 공개하지 않는다.
- [Python 공식 문서: 반복문의 else](https://docs.python.org/3/tutorial/controlflow.html#else-clauses-on-loops). 아래 개념 노트의 예시는 복습을 위해 새로 구성했다.
